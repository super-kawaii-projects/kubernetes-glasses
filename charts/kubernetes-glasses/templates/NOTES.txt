
  kubernetes-glasses has been installed.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

  Components:
  {{- if .Values.controller.enabled }}
    - controller  (watches the cluster API, port {{ .Values.controller.service.port }})
  {{- end }}
  {{- if .Values.frontend.enabled }}
    - frontend    (web UI, port {{ .Values.frontend.service.port }})
  {{- end }}
  {{- if .Values.daemonset.enabled }}
    - daemonset   (per-node agent, one pod per node)
  {{- end }}

  ─── Access the UI ───────────────────────────────────────────────

{{- if .Values.ingress.enabled }}
  Ingress is enabled. Browse to:
  {{- range .Values.ingress.hosts }}
    http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
  {{- end }}
{{- else }}
  No ingress configured. Port-forward the frontend to your machine:

    kubectl port-forward -n {{ .Release.Namespace }} \
      svc/{{ include "kubernetes-glasses.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}

  Then open:  http://localhost:{{ .Values.frontend.service.port }}
{{- end }}

  ─── Check status ────────────────────────────────────────────────

    kubectl get pods -n {{ .Release.Namespace }} \
      -l app.kubernetes.io/instance={{ .Release.Name }}

  It may take a few seconds for the controller to sync cluster state.
